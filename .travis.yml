language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: 
  - make
  - ctest -VV
  - ./scripts/generate_coverage.sh ../build

